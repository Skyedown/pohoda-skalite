# Gateway nginx — always running, never recreated during deploys.
# Static files are served from the frontend-static named volume,
# which is updated by the one-shot frontend build container.
FROM nginx:alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
